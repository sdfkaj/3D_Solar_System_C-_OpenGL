# Texture placeholders

The demo loads two textures from this folder:

- `sun.jpg` (diffuse map for the sun)
- `Earth (A).jpg` (diffuse map for the Earth model)

Place any appropriately licensed images here before running the application.
